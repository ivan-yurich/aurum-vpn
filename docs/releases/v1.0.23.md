# Aurum VPN Android v1.0.23

## Changes

- Fixed in-app APK installation after Android asks to allow installs from Aurum
  VPN.
- Added the required Android `FileProvider` configuration for downloaded update
  APK files.
- Open the Android package installer with the dedicated install action.

## Validation

- `flutter analyze`
- `flutter test`
- `flutter build apk --release --split-per-abi`
- `apksigner verify`
